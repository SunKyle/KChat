/**
 * KChat Design Tokens
 *
 * ⚠️ ARCHIVE / REFERENCE ONLY
 *
 * THE SOURCE OF TRUTH is `index.css` CSS custom properties (variables).
 * All runtime theming is controlled by CSS variables.
 *
 * This file exists as a TypeScript reference mirroring those CSS variable values
 * for IDE autocomplete and documentation purposes.
 *
 * When updating colors/sizes, update `index.css` FIRST, then sync this file.
 * See: frontend/src/index.css (:root and .dark blocks)
 */
